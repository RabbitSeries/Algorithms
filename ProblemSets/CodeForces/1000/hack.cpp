#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cout << 1 << endl;
    for (long long j = 0; j < 2500; j++) cout << 0;
    for (long long j = 0; j < 2500; j++) cout << 1;
    cout << endl;
    for (long long j = 0; j < 2500; j++) cout << 0;
    for (long long j = 0; j < 2500; j++) cout << 1;
    cout << endl;
}